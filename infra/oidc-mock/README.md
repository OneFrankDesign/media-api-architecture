# OIDC Mock

Compose uses `soluto/oidc-server-mock` with inline JSON configuration for deterministic local OIDC discovery, authorize, token, and JWKS endpoints.

The effective configuration lives in `/Media API/infra/compose/docker-compose.yml` under the `oidc-mock` service.
